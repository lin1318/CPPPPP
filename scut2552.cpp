#include<iostream>
#include<iomanip>
using namespace std;
double a[5]={2.98,4.50,9.98,4.49,6.87};
int main(){
	int i,j,k,n;
	double sum=0;
	switch (k);
	  while(cin>>j){
	  	if(j==-1){
	  	  cout<<fixed<<setprecision(2)<<sum<<endl;
	  	  sum=0;
	  }
		else{
			 cin>>k;
			 if(k<0) k=0;
			sum=sum+a[j-1]*k;
		 }  
	  }
}
